import 'package:flutter/material.dart';

class WorldState extends StatelessWidget {
  const WorldState({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
